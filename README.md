# Task Tracker app (React & Javascript & Vite)
React app exercise based on tutorial by Brad Traversy (created with Vite instead of Create React App).

To see the app in action, clone this repository and run:

npm install

npm start
